name: PR update

on:
  push:
    branches: main

jobs:
  autoupdate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run build
        run: yarn build
      - name: Update all PRs
        uses: ./
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
